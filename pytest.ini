[pytest]
pythonpath = server
markers =
    current: functional tests that verify the current CSM specification and supported workflow.
    future: progressive tests that document planned or forward-compatible behavior.
    regression: regression tests that protect previously fixed behavior, especially Office/Word and local API stability.
addopts = -ra -p no:cacheprovider
